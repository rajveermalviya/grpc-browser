version: '3'

services:
  go-grpc-backend:
    build: ./server

  envoy-grpc-proxy:
    build: ./server/proxy
    ports:
      - 8081:8080
    depends_on:
      - go-grpc-backend

  frontend-grpc:
    build: ./client
    ports:
      - 8080:80
    depends_on:
      - envoy-grpc-proxy
